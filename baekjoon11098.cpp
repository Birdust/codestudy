#include <iostream>
using namespace std;
#include "string"
int main() {
    char info[100][21] = {};
    int n, p, C, name;
    cin >> n >> p;
    for (int test = 0;test < n;test++)
    {
        for (int i = 0; i < p;i++)
        {
            cin >> C >> name;
            cin >> info[i];
            if (info[i] < max)
                
        }
        //max 

    }

    return 0;
}